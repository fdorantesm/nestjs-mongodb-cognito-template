meta {
  name: Create Permission
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/v1/permissions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "New Permission",
    "code": "NewModule:Action"
  }
}
