meta {
  name: Update Permission
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/v1/permissions/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Updated Permission Name",
    "code": "UpdatedModule:Action"
  }
}
