meta {
  name: Delete Permission
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/v1/permissions/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{accessToken}}
}
