meta {
  name: Create Role
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/v1/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "New Role",
    "code": "new_role",
    "permissions": []
  }
}
