meta {
  name: Update Setting
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/v1/settings
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "key": "setting_key",
    "value": "new_value"
  }
}
