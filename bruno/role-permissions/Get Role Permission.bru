meta {
  name: Get Role Permission
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/role-permissions/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{accessToken}}
}
