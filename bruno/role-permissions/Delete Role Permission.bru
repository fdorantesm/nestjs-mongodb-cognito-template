meta {
  name: Delete Role Permission
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/v1/role-permissions/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{accessToken}}
}
